# GraphQL workshop backend

This folder contains the backend code. Get started by installing the dependencies `npm install`. Create a `.env` file and insert the URL of your MongoDB with the key `MONGODB_URL`.
