import { Schema } from "mongoose";

export const PetModel = new Schema({
  // Exercise 3: Fill out the properties of the schema. You can find more information on how to
  //             define a schema in the docs: https://mongoosejs.com/docs/guide.html#definition
});
